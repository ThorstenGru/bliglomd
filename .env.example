# ── Frontend env vars (safe to inject at build time) ──────────────────────────
# Copy to .env.local and fill in real values. NEVER commit .env.local.
VITE_SUPABASE_URL=https://ydkahdqvuykpmjkpunck.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ── Edge Function secrets (set in Supabase Dashboard → Edge Functions → Secrets)
# NEVER put these in this file or commit them anywhere.
# RESEND_API_KEY=re_...
# DIGEST_SECRET=<uuid>  (stored in Supabase Vault, set via: supabase secrets set)
